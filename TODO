
to the webpage add into every clustor in clusters overview:

 * select button -> to generate export list to copypaste to utils...

into every image in cluster view:

 * the last button -> to mark (and export) tail of images to be recycled

